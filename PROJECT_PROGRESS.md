# SAIS Project Development Progress

## Phase 0 environment recovery — 2026-09-01

- Prisma CLI and Client upgraded together from 5.22.0 to 6.19.0.
- Prisma Client generation and schema validation pass.
- Runtime connectivity to the configured Neon PostgreSQL database is verified.
- All 29 checked-in migrations are applied successfully.
- The configured application owner is provisioned idempotently and real authentication succeeds as `APPLICATION_MANAGER` / `OWNER`.
- The transactional single-school development bootstrap creates current-schema school-admin, teacher, student, and parent accounts plus teacher/parent/student domain links.
- All four development roles authenticate successfully with their expected role claims.
- Verification baseline remains green: lint passes, backend tests report 182 passed and 1 intentional live-database skip, frontend tests report 2 passed, and backend/frontend production builds pass.
- Remaining Phase 0 gap: verify refresh, logout, password recovery, and protected-route navigation through browser-level tests and consolidate the setup/recovery runbook.

Last updated: 2026-09-01

This file tracks the implementation progress of the SAIS platform. `ROADMAP_TODO.md` remains the authoritative execution queue; update both files whenever a task status changes.

## Tracked task claims

> Reconciliation notice: rows EXM-001 through LMS-005 were imported from V0 and are retained as implementation evidence, but their authoritative status is `REVIEW`, not `DONE`. They do not yet satisfy the repository completion definition. See `docs/audit/v0-change-reconciliation-2026-08-27.md`.

| Order | ID          | Status | Summary                                                                                                                                                                                                                                                                                                                                                                              |
| ----: | ----------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|     1 | DB-001      | DONE   | Reconciled active Prisma schema, services, routes, and controlled unavailable features.                                                                                                                                                                                                                                                                                              |
|     2 | QA-001      | DONE   | Stabilized the backend test runner and verification workflow.                                                                                                                                                                                                                                                                                                                        |
|   2.1 | PARENT-001  | DONE   | Completed parent persistence and portal contract.                                                                                                                                                                                                                                                                                                                                    |
|   2.2 | TEACHER-001 | DONE   | Completed teacher persistence and lifecycle contract.                                                                                                                                                                                                                                                                                                                                |
|     3 | SUB-001     | DONE   | Completed subject domain persistence, CRUD, lifecycle APIs, and admin UI.                                                                                                                                                                                                                                                                                                            |
|     4 | CLS-001     | DONE   | Completed classes, sections, rooms, assignments, and enrollment workflows.                                                                                                                                                                                                                                                                                                           |
|     5 | ATT-001     | DONE   | Completed attendance rosters, bulk marking, locking, auditing, and UI.                                                                                                                                                                                                                                                                                                               |
|     6 | POL-001     | DONE   | Completed grading schemes, bands, weights, pass rules, and lifecycle APIs.                                                                                                                                                                                                                                                                                                           |
|     7 | EXM-001     | DONE   | Completed examination persistence, mark writes, lifecycle transitions, audits, and API mounting.                                                                                                                                                                                                                                                                                     |
|     8 | RES-001     | DONE   | Completed result persistence, processing, ranking, publication, and audit workflow.                                                                                                                                                                                                                                                                                                  |
|     9 | TTB-001     | DONE   | Added timetable, slots, schedule entries, conflicts, versions, audits, and substitutions.                                                                                                                                                                                                                                                                                            |
|    10 | FIN-001     | DONE   | Added finance fees, invoices, payments, and ledger transaction persistence.                                                                                                                                                                                                                                                                                                          |
|    11 | PAY-001     | DONE   | Added payment intents, payment attempts, and idempotent gateway webhook persistence.                                                                                                                                                                                                                                                                                                 |
|    12 | COM-001     | DONE   | Added notification events, deliveries, preferences, tenant-scoped service, and active routes.                                                                                                                                                                                                                                                                                        |
|    13 | HR-001      | DONE   | Added employees, HR departments and positions, leave requests, payroll runs/items, and active HR routes; Prisma validation, syntax checks, backend tests, and Neon table verification passed.                                                                                                                                                                                        |
|    14 | LIB-001     | DONE   | Activated tenant-scoped library overview, catalog search, and loans APIs; Prisma validation, syntax checks, backend suite, and diff checks passed.                                                                                                                                                                                                                                   |
|    15 | AST-001     | DONE   | Activated tenant-scoped assets and inventory overview/search APIs; Prisma validation, syntax checks, backend suite, and diff checks passed.                                                                                                                                                                                                                                          |
|    16 | TRN-001     | DONE   | Added transport vehicles, drivers, routes, stops, trips, inspections, active API mounting, and database persistence; validation and backend suite passed.                                                                                                                                                                                                                            |
|    17 | BRD-001     | DONE   | Completed dormitory, room, bed, application approval, allocation and checkout persistence, authenticated APIs, and operational UI; full suite passes (146 passed, 1 skipped).                                                                                                                                                                                                        |
|    18 | LMS-001     | DONE   | Added distinct digital-classroom and membership persistence, relational constraints, scoped lifecycle APIs on both mounts, and an operational classroom UI; full suite passes (151 passed, 1 skipped).                                                                                                                                                                               |
|    19 | LMS-002     | DONE   | Added migrated classroom announcements, posts and comments with membership-aware authorization, validated APIs on both mounts, and operational stream UI; full suite passes (156 passed, 1 skipped).                                                                                                                                                                                 |
|    20 | LMS-003     | DONE   | Added migrated digital-classroom assignments with optional school-scoped subjects, membership-aware reads, teacher/admin lifecycle controls, operational classwork UI, documentation, and passing verification (161 passed, 1 skipped).                                                                                                                                              |
|    21 | LMS-004     | DONE   | Added relational classroom materials, private authenticated upload/download delivery, member/teacher authorization, archive lifecycle, both active mounts, operational UI, migration, documentation, and passing verification (166 passed, 1 skipped).                                                                                                                               |
|    22 | LMS-005     | DONE   | Added authenticated student-owned submissions, assignment/classroom authorization, immutable serializable versions, controlled submit/retract lifecycle, same-classroom attachments, both API mounts, live UI, migration, docs, and passing verification (172 passed, 1 skipped).                                                                                                    |
|    23 | LMS-006     | DONE   | Added policy-linked quiz authoring, immutable published questions, protected answer keys, lifecycle controls, timed and limited student attempts, autosave, server scoring, both active mounts, shared live assessment UI, migration, docs, and passing verification (177 passed, 1 skipped).                                                                                        |
|    24 | LMS-007     | DONE   | Completed tenant-scoped rubric creation, publication and assignment; bounded criterion scoring; draft and released submission grades; persisted feedback threads; live teacher gradebook and student released-feedback views; both API mounts, documentation, and verification.                                                                                                      |
|    25 | LMS-008     | DONE   | Added member-authorized derived classroom calendars from published assignments and timetables, assignment-publication in-app notifications, API/UI integration, documentation, and a focused contract test. Workspace dependencies installed, full backend suite, frontend tests, lint, mobile typecheck, and diff check pass.                                                       |
|    26 | LMS-009     | DONE   | Connected TeacherDashboard, StudentHomeDashboard, ParentPortal, and ParentClassroomWorkspace to real LMS endpoints (`/lms/classrooms`, `/lms/assignments`, `/lms/calendar`, `/communication/unread-count`, `/parents/me`). Removed static demo data. All tests, lint, and typecheck pass.                                                                                            |
|    27 | LMS-010     | DONE   | Implemented relational `ClassroomLiveSession` model, non-destructive migration, lifecycle service, controller, and routes (`/api/lms/live-sessions`, `/api/v1/lms/live-sessions`). Connected `LiveLearningWorkspace.jsx` to live API endpoints and added unit contract tests.                                                                                                        |
|    28 | ANA-001     | DONE   | Calculated real operational analytics metrics (students, staff ratios, attendance %, fee collection %) and learning analytics from backend persistence; mounted authenticated `/api/analytics` & `/api/v1/analytics` routes; updated `AnalyticsDashboard.jsx` & `LearningAnalyticsWorkspace.jsx`.                                                                                    |
|    29 | SRCH-001    | DONE   | Implemented permission-aware global classroom search (`searchService.js`, `searchController.js`, `searchRoutes.js`) across digital classrooms, assignments, materials, announcements/stream posts, and subjects with role & membership authorization filtering. Mounted on `/api/lms/search`, `/api/v1/lms/search`, `/api/search`, `/api/v1/search`. Added `searchContract.test.js`. |

## Current progress

| Measure                 | Count | Percentage |
| ----------------------- | ----: | ---------: |
| Total atomic tasks      |    48 |       100% |
| Done                    |    31 |        65% |
| In review               |     0 |         0% |
| In progress             |     0 |         0% |
| Ready                   |     0 |         0% |
| Blocked by dependencies |    17 |        35% |

## Next work

Tasks 1 through 29 (DB-001 through SRCH-001) are complete. Remaining tasks 30-46 are currently BLOCKED by SEC-001 / AI-001 dependencies.

Timetable follow-up checkpoint (2026-09-06): Phase 2 domain reconciliation is complete as an extension of TTB-001. Added timetable settings, rooms/resources, teaching assignments, subject period requirements, expanded availability, normalized timetable references, additive migration `20260906100000_timetable_domain_reconciliation`, and contract coverage. Prisma validation and 4 focused timetable tests pass. The timetable generator, settings APIs, teacher-derived views, classroom/student authorization views, notifications, and final audit remain unfinished.

Phase 3 checkpoint (2026-09-06): Added school-scoped timetable settings read/upsert endpoints and deterministic settings-driven slot generation with validation for hours, working days, lesson duration, break/lunch boundaries, overlap, and workload limits. Focused timetable tests: 7 passed; Prisma validation and backend build pass. Prisma client generation is blocked by a Windows EPERM lock on the Prisma query-engine DLL, likely held by a running backend process; no source failure was reported.

Phase 4 checkpoint (2026-09-06): Persisted settings-generated slots into draft/review timetables through `POST /api/timetables/:id/generate-slots` and `/api/v1`, blocked regeneration when entries exist, added audit logging and an admin dashboard action. Timetable tests (7), frontend tests (10), backend build, and frontend build pass.

Phase 5 checkpoint (2026-09-06): Added teacher teaching-assignment CRUD with tenant/school-scoped validation across teacher, subject, class, academic year, and term; composite uniqueness prevents duplicate assignments; `GET /api/timetables/teachers/:teacherId/workload` reports total periods per week by subject and class. Focused timetable tests (7) and backend build pass. Next timetable stage: subject period requirements.

Phase 6 checkpoint (2026-09-06): Added subject period requirements CRUD, partial updates, school/tenant and academic term validation, period bounds and per-class/year/term capacity checks against generated teaching slots. Settings changes revalidate requirements; serializable transactions protect concurrent capacity writes. Fixed generated slots to respect daily limits, breaks and complete lesson durations, and corrected the settings compound-key lookup. Focused timetable tests (14) and backend build pass. Scheduling feasibility, laboratory/stream placement and UI management remain later work; no live database migration was applied.

Phase 7 checkpoint (2026-09-06): Added scoped recurring teacher availability CRUD with AVAILABLE/UNAVAILABLE/PREFERRED rules, partial updates and weekly window validation. Manual lessons enforce availability over their complete duration; publication/locking revalidate current rules; availability edits protect non-archived lessons. Related writes run in serializable transactions. Focused timetable tests (22), full backend suite (238 passed, 1 skipped), backend build and targeted lint pass. No migration applied; one-off dates, substitutions and availability UI remain later work. Next: Phase 8 room/resource management, then global generation.

## Verification baseline

Completed vertical slices have been checked with Prisma validation/generation, JavaScript syntax checks, backend tests, and `git diff --check` where applicable. Before declaring future work complete, run the relevant focused tests plus the full project validation suite.

## Update rule

Whenever implementation proceeds:

1. Update the task status and work log in `ROADMAP_TODO.md`.
2. Update this file's completed/current progress section.
3. Record verification evidence.
4. Commit and push both tracking and implementation changes together.
5. Keep this file synchronized with the authoritative roadmap.

Phase 8 checkpoint: Added scoped room/resource CRUD and room capacity, active-state, laboratory and overlap checks for lesson creation/publication. Added readable timetable options and an editable SSS Science 3A weekly preview (40 periods, nine subjects, five core subjects at six periods each). Configured database read returned no schools/classes/academic years, so the draft remains explicitly unsaved and additional subjects provisional pending user details. Backend: 241 passed, 1 skipped; frontend: 12 passed; builds and targeted lint pass. No migration applied. See docs/drafts/sss-science-3a-timetable.md.

School setup checkpoint: User confirmed Aunty Isha Internation School and First Term and authorized setup. Applied the pending additive timetable migration, then saved tenant/school, 2026/27 academic year, First Term, SSS Science 3A, nine subjects and requirements, 50 slots (40 lessons plus breaks/lunch), version and audit in one transaction. Read-back verified core subjects at six periods each and additional subjects at 3/3/2/2. Draft remains unpublished, with teachers/rooms unassigned; dates and capacity are documented planning assumptions. Reusable setup: backend/scripts/setup-aunty-isha-timetable.mjs.

Timetable revision: removed the separate break from the saved SSS Science 3A draft and school settings. Lunch is now 12:00-12:40; 40 lessons retained, 45 total slots, school day unchanged at 08:00-14:00. Version snapshot and audit saved. Preview/document updated; frontend build, 12 tests and targeted lint pass.

Timetable Phase 9 checkpoint: added real room/resource management to the timetable dashboard, including classroom/laboratory/resource creation, capacities, resource labels, active-state controls and guarded empty-school loading. The UI calls the existing validated room API. After the explicit tenant purge, no replacement scheduling identities were fabricated. Focused timetable contracts (10), frontend tests (13), and backend/frontend builds pass. Next: teaching-assignment and availability UI, then global generation.

Timetable Phase 10 checkpoint: added school-scoped teachers to timetable options, real teaching-assignment UI for teacher/subject/class/term workloads, and recurring teacher availability UI with available, unavailable, and preferred windows. Existing protected CRUD APIs back all actions; no staff data is fabricated. Focused timetable tests (15), frontend tests (13), frontend build, and targeted lint pass. Next: deterministic global generation and feasibility reporting.

Timetable Phase 11 checkpoint: added atomic deterministic generation for empty drafts using subject requirements, active teaching assignments, teacher availability, class capacity, active rooms/labs, and persisted slots. The generator enforces teacher/class/room occupancy and adjacent double periods, fails with detailed feasibility issues without partial writes, and records successful generation in the audit log. UI action, focused contracts, builds, lint, and frontend tests pass. Next: preference scoring, balanced distribution, and manual entry editing.

Timetable Phase 12 checkpoint: deterministic candidate scoring now spreads subjects across weekdays, penalizes concentrated class and teacher daily loads, honors recurring preferred teacher windows, preserves double periods, and uses stable weekday/time tie-breaking. Added focused scoring tests. Seventeen timetable tests, targeted lint, and backend build verification pass. Next: controlled manual entry editing and hard workload limits.

Timetable Phase 16 checkpoint (2026-09-07): added authenticated, school-scoped `GET /api/timetables/:id/readiness` and `/api/v1/timetables/:id/readiness` reports. The read-only endpoint identifies missing teaching slots, subject requirements, active rooms, teacher assignments, entry teachers/rooms, period mismatches, unresolved hard conflicts, invalid lesson spans and teacher workload-limit breaches. `TimetableReadinessPanel` now shows server-verified blockers in the dashboard. It does not create, edit, publish or lock timetable data. Verification: 25 focused backend timetable tests, backend build and frontend build pass. Live release verification remains dependent on configuring real teachers, assignments, availability and rooms for the explicitly selected school.

Timetable Phase 17 checkpoint (2026-09-07): added the read-only `npm run timetable:readiness` operational command. It validates `SINGLE_SCHOOL_ID`, runs the server readiness service for every timetable in the configured school, reports blockers and returns a nonzero exit code when configuration or readiness fails. The live check found that the configured school ID does not exist and that the connected database currently contains no school records, so activation cannot continue without creating or restoring the intended real school. No database records were changed.

Timetable Phase 18 checkpoint (2026-09-07): added safe, idempotent single-school bootstrap before the backend listens. `SINGLE_SCHOOL_ID` and `SINGLE_SCHOOL_NAME` must be configured together; an empty database receives an active umbrella tenant and its one configured school, repeat starts reuse the exact school, and mismatched IDs or additional school records fail startup instead of creating duplicates. Added `npm run school:bootstrap` for explicit operational verification and focused tests. The live environment was reconciled to the existing Aunty Isha International Academy record (`492e874f-d090-4b72-a84d-3e79070fbe3d`); bootstrap reuses it successfully. The school currently has no timetables, so timetable readiness remains blocked pending academic and timetable setup.

Timetable Phase 19 checkpoint (2026-09-07): restored the documented 2026/27 SSS Science 3A academic foundation to Aunty Isha International Academy through an idempotent, serializable bootstrap. The current database now has the active 2026/27 academic year (September 1, 2026-August 31, 2027), First Term (September 1-December 31, 2026), SSS3 grade level, planned SSS Science 3A class with capacity 40, nine active subjects and all nine class-subject links. Added `npm run timetable:bootstrap-academics`, focused tests and a bounded 30-second transaction timeout for remote database latency. Teachers, rooms, subject-period requirements and the timetable itself were not fabricated and remain for later phases.

Timetable Phase 13 checkpoint (2026-09-06): automatic generation now reads school settings inside its serializable transaction and enforces teacher daily, weekly and consecutive-period limits across classes in the draft. Double lessons count as two periods; breaks interrupt consecutive runs. Feasibility errors identify limiting workload rules and write no partial entries or audit. Verified 25 focused timetable tests, backend build and targeted lint. No database migration or live-data verification. Next: controlled manual entry editing.

School management repair (2026-09-06): reconciled list/search/create/update with the active flat School model. The API preserves `slug` as an alias for persisted `code`; removed nonexistent filters and nested relations. Authenticated tenant identity cannot be replaced through query parameters or headers. Failed list requests now display an unavailable state and retry action, without claiming zero schools. Read-only verification against the configured database returned one school. No migration or school-data changes were applied.

Owner activation visibility (2026-09-06): restored /owner/activations as an APPLICATION_MANAGER-protected review screen instead of redirecting to administration. Added a persistent administration notification with the live pending count (refreshes every 30 seconds and on window focus), plus a school-management shortcut. Review displays applicant identity, submission/expiry dates, activation/rejection actions and persisted decision/error feedback. Uses existing authenticated activation APIs and owner enforcement. Existing seven-day eligibility is unchanged; this workflow activates school administrator registrations, not a School status field. Verification: 20 frontend tests, production build and targeted lint passed. No activation decisions were made against live accounts.

Single-school and branches correction (2026-09-07): removed owner school switching and client-selected school context. Main-school creation rejects a second School in a serializable transaction. School management edits the configured main school and adds/renames branches backed by the existing Campus model, with generated branch codes. Main-school resolution uses the configured school globally and ignores client school-selection headers. Existing school records were preserved. Database inventory found Aunty Isha Internation School and Medishatu International School; configuration awaits the user's choice of main school. No existing record was converted to a branch, no migration applied, and no live data changed. Automatic identifiers from the preceding work remain in place.

Timetable Phase 14 checkpoint (2026-09-07): added a manual lesson editor for draft/review timetables using named classes, subjects, teachers, rooms and periods. Added scoped PATCH /api/timetables/:id/entries/:entryId (also /api/v1). Creation and editing validate entity scope, adjacent lesson spans, full-span class/teacher/room overlap, recurring availability and hard teacher workloads. Edits preserve omitted values, avoid colliding with themselves, clear stale teaching-assignment links when identities change, refresh conflicts and audit ENTRY_UPDATED atomically. Published/locked timetables reject edits. Both timetable panels now use the shared authenticated client. Verification: 27 focused backend tests, 24 frontend tests, both builds and targeted lint pass. No live timetable was changed; main-school configuration remains unresolved. Next: publication-time workload reconciliation and live generation/editing verification.

Timetable Phase 15 checkpoint (2026-09-07): publication and locking load current school settings inside the existing serializable transaction and recheck teacher daily, weekly and consecutive-period workloads across all timetable classes. Double lessons count fully; breaks/day boundaries interrupt consecutive teaching. Failures occur before status/version/audit writes and surface a publish/lock-specific validation message. Missing settings use the same defaults as generation/manual editing. Unassigned lessons and BREAK/FREE entries do not contribute teacher workload. Archiving remains available. Verification: 32 focused backend tests, backend build and targeted lint pass. No schema migration or live timetable changes. Live verification remains pending designation of the main school.

Timetable live readiness checkpoint (2026-09-07): read-only database verification found one Aunty Isha SSS Science 3A draft with 40 valid lesson spans, 40 teaching slots, five break/lunch slots and exact totals for all nine subject requirements. The conflict detector returned no conflicts in the unstaffed draft. All 40 lessons lack teacher and room assignments; the school has no teachers, timetable rooms or teaching assignments. Medishatu has no timetable setup. Main-school designation remains pending. No live records changed. See docs/audit/timetable-live-readiness-2026-09-07.md. End-to-end generation/editing/publication verification remains incomplete until these prerequisites are supplied.

SEC-001 Phase 9 checkpoint (2026-09-08): sign-in no longer reveals whether an identity is missing, suspended, pending activation or temporarily locked. All of those cases return the same authentication error, while internal login-attempt records retain the specific forensic reason. Missing identities now perform a dummy bcrypt comparison to reduce timing-based enumeration. The focused regression, backend build, targeted lint and diff check pass. The full backend suite reports 336 passed, one intentional live-database skip and three pre-existing subject/class contract failures unrelated to this security change. SEC-001 remains in progress.

SEC-001 Phase 10 checkpoint (2026-09-08): successful login bookkeeping and credential issuance are atomic. User success state, append-only success telemetry, the device session, initial refresh token, remembered device and `LOGIN_SUCCEEDED` audit now share the session issuance transaction. An audit or persistence failure rolls everything back and returns no credentials. Nine focused auth/session tests, backend build, targeted lint and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 11 checkpoint (2026-09-08): invalid-password counter and lockout updates now commit atomically with append-only attempt telemetry and the corresponding login audit in a serializable transaction. The account is re-read inside the transaction so the decision uses current persisted state. Regression coverage proves rollback for account, attempt and audit write failures. Thirteen focused authentication/session tests, backend build, targeted lint and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 12 checkpoint (2026-09-08): login now enforces persisted recent-failure limits for both the submitted email and the client IP before looking up credentials. The IP limit defaults to 25 within the existing 15-minute lockout window, is configurable through `MAX_FAILED_LOGINS_PER_IP`, and remains effective across process restarts and horizontally scaled instances. Fourteen focused auth/session tests, backend build, targeted lint and diff check pass. Configuration examples and backend documentation are updated. SEC-001 remains in progress.

SEC-001 Phase 13 checkpoint (2026-09-08): explicit brute-force thresholds and lockout duration must be positive safe integers. Zero, negative, fractional, malformed and unsafe values fail startup, while omitted settings retain documented defaults. Fifteen focused authentication/configuration/session tests, backend build, targeted lint and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 14 checkpoint (2026-09-08): client IP attribution no longer trusts `x-forwarded-for` directly. Request context, audit metadata and rate-limit keys use Express-resolved `req.ip`, so forwarded addresses are honored only under the configured `trust proxy` policy and cannot be trivially forged on direct deployments. Eighteen focused authentication/configuration/request-context tests, backend build, targeted lint and diff check pass. Proxy configuration guidance is documented. SEC-001 remains in progress.

SEC-001 Phase 13 checkpoint (2026-09-08): explicitly configured brute-force thresholds and lockout duration now fail startup unless they are positive safe integers. This prevents zero, negative, fractional, malformed or numerically unsafe values from silently disabling lockout timing or denying all login attempts. Omitted variables retain their documented defaults. Fifteen focused authentication/configuration/session tests, backend build, targeted lint and diff check pass. Backend configuration documentation is updated. SEC-001 remains in progress.

SEC-001 Phase 15 checkpoint (2026-09-08): login throttling now uses one canonical identity across the HTTP and persisted limiter layers. IPv6 clients are grouped by their /64 allocation so rotating interface addresses cannot reset counters; IPv4 and IPv4-mapped addresses remain address-specific. Login-attempt records use the throttle identity while session and security-audit evidence retains the complete Express-resolved address. Nineteen focused authentication and request-context tests, backend build, targeted lint and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 16 checkpoint (2026-09-08): closed unauthenticated role self-provisioning. Public registration now accepts and independently enforces only the approval-based `TENANT_ADMIN` application type; staff, teacher, parent and student identities remain available only through authenticated administrator provisioning. The web signup screen no longer submits staff account creation and explains how to obtain an issued account. Four focused backend tests, all 37 frontend tests, backend/frontend production builds, targeted lint and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 17 checkpoint (2026-09-08): removed the duplicate JavaScript backend bootstraps and made the compiled TypeScript application factory the only composition root for development, npm production startup, Docker and serverless delivery. Docker now builds `backend/dist`, starts `dist/main.js` and probes `/api/v1/health/live`. The canonical app preserves explicit CORS rejection, system/documentation/deferred routes and configured proxy trust, while request IDs/logging now cover rejected origins. The 25-test route/composition suite, backend build, compiled-artifact verification, targeted lint and diff check pass. The full backend suite reports 369 passed, one intentional live-database skip and one pre-existing class source-contract failure outside this checkpoint. Docker is not installed locally, so its definition has static regression coverage but no local image build. SEC-001 remains in progress for the application-wide authorization/school-scope audit and compliance controls.

UI contrast checkpoint (2026-09-08): corrected the cascade conflict between the light design system and the final global dark theme. Authentication now has an explicit light boundary with readable headings, labels, inputs, placeholders and links. Shared `.page-shell` routes now use aligned dark tokens and surfaces for data panels, tables, cards and the student-registration modal. Two WCAG contrast regressions, all 40 frontend tests, the production build, targeted lint and diff check pass. Browser screenshot verification remains pending because no browser-control surface was available. See `docs/audit/ui-contrast-remediation-2026-09-08.md`.

SEC-001 Phase 18 checkpoint (2026-09-08): authorization audit wave 1 corrected undefined permission codes on active student and school-administrator routes, added a repository-wide regression that rejects route permission literals missing from the canonical catalog, and added explicit administrator/teacher boundaries to digital-classroom membership and archival mutations. The service still enforces selected-school ownership and administrator-or-classroom-owner authorization. Eight focused tests, backend build, targeted lint and diff check pass. The full backend suite reports 372 passed, one intentional live-database skip and the same pre-existing class source-contract failure. SEC-001 remains in progress for remaining mounted-route and compliance audits.

SEC-001 Phase 19 checkpoint (2026-09-08): authorization audit wave 2 prevents analytics controllers from discarding the resolved single-school context. Overview, KPI, learning and export operations now require tenant and school scope; subject and class analytics queries include both, and exports require administrator roles. The stale class source contract was aligned with its stronger tenant, school and soft-delete lookup. Eighteen focused tests, backend build, targeted lint and diff check pass. The complete backend suite passes with 374 tests and one intentional live-database skip. SEC-001 remains in progress for remaining mounted-route and compliance audits.

SEC-001 Phase 20 checkpoint (2026-09-08): authorization audit wave 3 removed the provisional billing router from the canonical server. Both billing API prefixes now return controlled `501 FEATURE_NOT_IMPLEMENTED` responses naming `SaaS-001`; incomplete subscription lifecycle mutations and unsigned provider webhook ingestion are unreachable until durable persistence and verification are implemented. Seventeen focused tests, backend build, targeted lint and diff check pass. The complete backend suite passes with 375 tests and one intentional live-database skip. SEC-001 remains in progress for remaining mounted-route and compliance audits.

SEC-001 Phase 21 checkpoint (2026-09-08): authorization audit wave 4 restored the approval-only registration workflow on both canonical activation API prefixes. Routes and services require both application-manager account type and owner platform role; decisions validate UUID plus explicit approve/reject input, and approval conditionally updates only current non-deleted pending applicants. The owner seed preserves both identity attributes. Payment webhook signature, ownership, idempotency and transaction controls were reviewed and retained. Five focused tests, nineteen combined tests, backend build, targeted lint and diff check pass. The complete backend suite passes with 378 tests and one intentional live-database skip. SEC-001 remains in progress for remaining mounted-route and compliance audits.

SEC-001 Phase 22 checkpoint (2026-09-08): authorization audit wave 5 constrains the parent portal to the configured school. Parent identity resolution rejects another school's parent record; visible relationships and link targets require an active class enrollment in the configured school, and self-requested links remain pending. Correctly shaped strict profile/link schemas and unlink UUID validation restore safe parent mutations. Eight focused tests, backend build, targeted lint and diff check pass. The complete backend suite passes with 378 tests and one intentional live-database skip. SEC-001 remains in progress for remaining mounted-route and compliance audits.

Class setup refinement (2026-09-09): completed the requested year-and-stage class workflow under CLS-001. Administrators select a starting year from a year-only control and one of four canonical stages: Pre-School Nursery, Primary School, Junior Secondary, or Senior Secondary. The API rejects unknown stages, reuses an existing tenant academic year beginning in the selected year or provisions a calendar-year record, creates/reuses the scoped grade level, and persists the class in one transaction. Six focused backend tests, one focused frontend behavior test, both production builds, targeted lint and diff check pass. The full backend suite reports 380 passed and one intentional database skip; the full frontend suite reports 41 passed. SEC-001 remains in progress.

SEC-001 Phase 23 checkpoint (2026-09-09): authorization audit wave 6 separates examination and result reading from lifecycle administration. Teachers may read school-scoped exams/results and enter marks only when their active teacher identity has an active teaching assignment for the candidate class and subject. Only platform and school administrators may create exams, manage candidates/schedules, transition exam status, process results, or approve/publish/lock results. Candidate identity lookup now includes tenant, school and soft-delete scope, and examination detail identifiers are UUID-validated. Twenty-one focused tests, backend build, targeted lint and diff check pass. The complete backend suite reports 386 passed and one intentional live-database skip. SEC-001 remains in progress for remaining mounted-route and compliance audits.

SEC-001 Phase 24 checkpoint (2026-09-09): authorization audit wave 7 constrains attendance to assigned classes. Teachers see and manage attendance only for classes linked through active class-teacher membership or active teaching assignments; platform and school administrators retain school-wide access. Read, create, detail, bulk mark, and status paths enforce the same service boundary. Session identifiers are UUID-validated, and session creation now persists the authenticated actor through the correct `createdById` field. The attendance screen no longer requests School UUID or Class UUID values: school scope comes from authentication and eligible classes come from a protected options endpoint. Eight focused backend tests and one focused frontend behavior test pass. The complete backend suite reports 390 passed and one intentional live-database skip; the complete frontend suite reports 42 passed. Both production builds, targeted lint, formatting and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 25 checkpoint (2026-09-09): authorization audit wave 8 hardens canonical student administration at the HTTP boundary. List, detail, create, update and guardian requests now use strict schemas with bounded pagination, UUID validation, controlled fields and future-date rejection. Caller-supplied tenant, school and ownership fields are rejected, and the controller derives tenant scope only from the authenticated school context. The student dashboard replaces fabricated enrollment and review figures with the persisted total and visible-record count. Five focused backend tests and one focused frontend behavior test pass. The complete backend suite reports 393 passed and one intentional live-database skip; the clean full frontend rerun reports 42 passed. Both production builds, targeted lint, formatting and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 26 checkpoint (2026-09-09): authorization audit wave 9 hardens canonical teacher administration. The teacher self-profile route now resolves the authenticated single-school context before lookup, and user-linked teacher lookup requires tenant and school ownership. Administrator list, detail, create and lifecycle routes retain school scope, include platform-owner administration, validate UUIDs, reject unknown lifecycle states and caller-controlled ownership fields, and cap directory pagination at 100 records. The teacher workspace no longer reads or sends a client-selected school header. The focused five-test backend teacher suite and one frontend contract test pass. The complete backend suite reports 395 passed and one intentional live-database skip; the complete frontend suite reports 43 passed. Both production builds, targeted lint, formatting and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 27 checkpoint (2026-09-09): authorization audit wave 10 closes cross-tenant and ownership gaps in canonical user management. User detail, update, status, delete, restore, profile and image routes now validate UUIDs and strict active-schema payloads. Profile reads and transactional profile updates require the authenticated tenant, while profile-image replacement/removal verifies the tenant-owned target inside the same transaction as the image and audit writes. Push-token registration accepts only validated device fields and binds `userId` exclusively to the authenticated account. Preference inputs are mapped into the active JSON settings column while retaining existing settings. List DTOs now expose safe account-type/name fields, and the frontend offers only supported non-administrator account types and labels page-limited statistics as visible records. Six focused backend tests and one focused frontend contract test pass. The complete backend suite reports 399 passed and one intentional live-database skip; the complete frontend suite reports 44 passed. Both production builds, targeted lint, formatting and diff check pass. SEC-001 remains in progress.

SEC-001 Phase 28 checkpoint (2026-09-09): authorization audit wave 11 aligns school administration with the active single-school schema. School and Campus branch requests now use strict bounded schemas and UUID validation, while controller-level tenant derivation prevents client scope overrides. The obsolete school-context query and generic child-resource routes targeting nonexistent persistence fields/models are no longer mounted. The configured main school cannot be deleted, and administrator-assignment routes require `schools.assign` before returning a controlled `501 FEATURE_NOT_IMPLEMENTED` response tied to RBAC-002 until durable persistence exists. Corrupted replacement characters were also removed from branch loading/saving text. Eleven focused backend tests and one focused frontend contract test pass. The complete backend suite reports 401 passed and one intentional live-database skip; the complete frontend suite reports 45 passed. Both production builds pass. SEC-001 remains in progress.

SEC-001 Phase 29 checkpoint (2026-09-09): canonical subject administration now validates UUID identifiers on detail, edit, delete and status routes; rejects unknown list-query and nested assignment fields; and rejects empty updates. Eight focused tests and the full backend suite pass (402 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 30 checkpoint (2026-09-09): class administration now validates route UUIDs, rejects unknown request and nested subject fields, bounds pagination and validates list lifecycle states. Controllers preserve authenticated tenant/school scope, actor and route identities after applying client input. Ten focused class tests and the full backend suite pass (406 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 31 checkpoint (2026-09-09): academic-period requests now reject unknown fields; controllers preserve authenticated context and route identity; year, term and event lifecycle writes include ownership predicates. Calendar creation sends only endpoint-supported fields. Full backend suite: 410 passed, one intentional live-database skip. Two focused frontend tests, both production builds, targeted lint, formatting and diff checks pass. SEC-001 remains in progress. The pre-existing academic-year CLOSED-state persistence limitation is documented in docs/security/security-administration-phase31.md.

SEC-001 Phase 32 checkpoint (2026-09-09): parent profile and unlink writes enforce parent tenant/school ownership and non-deleted state; link requests verify parent and student through the same transaction as persistence; portal relationships explicitly require student tenant ownership. Missing service scope fails closed. Ten focused tests and a clean full backend rerun pass (415 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 33 checkpoint (2026-09-09): academic-policy creation validates referenced subject tenant/school ownership within its persistence transaction. Detail UUID validation, strict top-level/nested schemas and scoped lifecycle writes close request-boundary gaps. Seven focused tests and the full backend suite pass (420 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 34 checkpoint (2026-09-09): communication schemas reject unknown fields and malformed quiet-hour values; services require tenant/school context; recipient ownership checks share the event/delivery transaction. Nine focused tests and the full backend suite pass (425 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 35 checkpoint (2026-09-09): finance writes persist only supported tenant/school context fields, balance updates retain ownership, request schemas reject unknown fields and transaction listing validates limits. Seven focused tests and the full backend suite pass (429 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 36 checkpoint (2026-09-09): existing payment idempotency keys require matching amount, provider and reference as well as tenant, school and invoice. Conflicting retries return 409 before writes; equivalent decimal representations remain accepted. Eight focused tests and the full backend suite pass (433 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 37 checkpoint (2026-09-09): invoice student and optional active fee eligibility checks now share the invoice creation transaction; invalid amounts fail before opening the transaction. Nine focused tests and the full backend suite pass (438 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 38 checkpoint (2026-09-09): payment transactions conditionally claim the scoped invoice balance before creating payment and ledger records; stale balance claims return 409. Eleven focused tests and the full backend suite pass (441 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. Live concurrency verification remains outstanding; SEC-001 remains in progress.

SEC-001 Phase 39 checkpoint (2026-09-09): payment amounts require positive two-decimal values within the ledger integer limit; service and HTTP validation share a predicate; balance subtraction and ledger values use validated minor units. Fourteen focused tests and the full backend suite pass (444 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 40 checkpoint (2026-09-09): invoice subtotal and discount require two-decimal nonnegative values within Decimal(12,2) bounds, with discount no greater than subtotal. Domain totals use integer minor-unit subtraction. Sixteen focused tests and the full backend suite pass (448 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. SEC-001 remains in progress.

SEC-001 Phase 41 checkpoint (2026-09-09): failed concurrent payment submissions re-read the idempotency key after transaction exit and return only a matching committed payment. Ownership/payload mismatches remain conflicts; unrelated persistence errors propagate. Eleven focused tests and the full backend suite pass (448 passed, one intentional live-database skip), including all four new race-recovery tests. Backend build, targeted lint, formatting and diff checks pass. Live database race verification remains outstanding; SEC-001 remains in progress.

SEC-001 Phase 42 checkpoint (2026-09-09): HR services reject missing school ownership; controllers preserve authenticated ownership and approver identity; employee and leave reference checks share creation transactions; employee, leave and payroll initial status and scope remain server-controlled. Strict employee queries and identifier objects reject unknown fields. Twelve focused HR tests and the full backend suite pass (459 passed, one intentional live-database skip). Backend build, targeted lint, formatting and diff checks pass. No schema or live HR/payroll data changes. SEC-001 remains in progress; live concurrency verification and payroll monetary limits remain outstanding.
